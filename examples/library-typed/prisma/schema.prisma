datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id        BigInt   @id @default(autoincrement())
  email     String
  name      String?
  metadata  Json
  createdAt DateTime @default(now()) @map("created_at")

  @@map("users")
}
