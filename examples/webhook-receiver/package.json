{
  "name": "@walcast/example-webhook-receiver",
  "version": "0.0.0",
  "private": true,
  "description": "A stdlib HTTP receiver for @walcast/sink-webhook: verify the HMAC signature, dedupe on event.id, return 2xx.",
  "type": "module",
  "scripts": {
    "start": "node index.mjs"
  },
  "dependencies": {
    "@walcast/sink-webhook": "workspace:*"
  }
}
